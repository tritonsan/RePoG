# Lite Campaigns

Copy `../templates/campaign/` into a new folder here to start a Lite campaign.

Suggested naming:

```text
campaigns/<campaign_id>/
```

Keep campaign ids short, lowercase, and readable. Prefer display-name-first
filenames inside campaign notes instead of raw engine ids.

