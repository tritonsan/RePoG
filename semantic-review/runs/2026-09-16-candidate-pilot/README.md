# Candidate pilot — 16 September 2026

This archive contains **synthetic** fixtures and exact generated responses:
36 RPG turns and 12 Companion exchanges. No real user's campaign or private
conversation was used. The actor read one next input at a time and did not see
the evaluator-only expectations. Separate evaluation is recorded alongside the
transcripts. Raw responses were not repaired after evaluation.

Read `manifest.json` for source commits, instruction hashes, artifact hashes,
local structural results, and the successful four-job CI record. The final
product commit is `5bd1b3c`; the first pilot segment began before the small
documented follow-up change.

The runtime mode is **fiction_only**. Restarts are synthetic owner reloads with
retained model context. These runs do not pass fresh-context or runtime
persistence lanes. No latency, token usage, baseline comparison, or real-user
enjoyment result was measured. They do not replace the official short-scenario
suites, which were not executed in this run.

Evaluator file references beginning with `tmp/evaluation-2026-09-16/` identify the
original execution location; the same remaining relative paths are archived
here. `responses/NN.json` contains the exact response for that turn; `owners/`
contains the final synthetic handoff, and `checkpoints/` contains frozen earlier
handoffs. These files are evaluation evidence, never playable campaign authority.

The archive is tracked in the source repository and excluded from player ZIPs.
