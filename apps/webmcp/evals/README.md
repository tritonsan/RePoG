# WebMCP evals

Run `npm run eval:webmcp` to regenerate the machine-readable and human-readable reports. The suite exercises discovery, autonomous turn-loop directives, cross-genre schema parity, character privacy, and authority boundaries.
